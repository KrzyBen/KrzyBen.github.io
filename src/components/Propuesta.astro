<section id="propuesta" class="propuesta-section">
  <div class="container">
    <div class="header">
      <h2>Nuestra propuesta: SRAACV</h2>
      <p class="subtitle">
        Un sistema inteligente que usa tu celular para detectar conductas peligrosas al volante 
        y generar reportes automáticos en tiempo real
      </p>
    </div>

    <div class="how-it-works">
      <h3 class="section-title">¿Cómo funciona?</h3>
      <div class="steps">
        <div class="step">
          <div class="step-number">1</div>
          <div class="step-content">
            <h4>Detección automática</h4>
            <p>La aplicación usa la cámara del celular (o una dashcam) y un modelo de IA ligero para detectar en tiempo real frenazos bruscos, cambios de carril sin señalizar y posibles colisiones.</p>
          </div>
        </div>

        <div class="step">
          <div class="step-number">2</div>
          <div class="step-content">
            <h4>Graba evidencia</h4>
            <p>Al detectar un evento, guarda automáticamente un video de 30 segundos con ubicación GPS, fecha y hora.</p>
          </div>
        </div>

        <div class="step">
          <div class="step-number">3</div>
          <div class="step-content">
            <h4>Análisis y reporte</h4>
            <p>Envía el video al servidor, donde se genera un reporte claro que indica qué ocurrió y si corresponde a una infracción a la Ley de Tránsito.</p>
          </div>
        </div>
      </div>
    </div>

    <div class="advantages-box">
      <h3>¿Por qué SRAACV es diferente?</h3>
      <div class="advantages-grid">
        <div class="advantage">✅ <strong>100% gratis</strong> para el conductor</div>
        <div class="advantage">✅ Enfocado exclusivamente en Chile</div>
        <div class="advantage">✅ Funciona con procesamiento híbrido</div>
        <div class="advantage">✅ Reportes con valor legal y referencias a la normativa vigente</div>
        <div class="advantage">✅ Ayuda a prevenir accidentes antes de que ocurran</div>
      </div>
    </div>
  </div>
</section>

<style>
  .propuesta-section {
    padding: 6rem 0;
    background: linear-gradient(135deg, #1e2937, #334155);
    color: white;
  }
  .container {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }
  .header {
    text-align: center;
    max-width: 800px;
    margin: 0 auto 4rem;
  }
  h2 {
    font-size: 3rem;
    font-weight: 700;
    margin-bottom: 1rem;
  }
  .subtitle {
    font-size: 1.5rem;
    color: #93c5fd;
    line-height: 1.4;
  }
  .section-title {
    font-size: 1.6rem;
    text-align: center;
    margin-bottom: 2.5rem;
    color: #60a5fa;
    font-weight: 600;
  }

  /* === Pasos (con hover) === */
  .steps {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }
  .step {
    background: rgba(255,255,255,0.08);
    border: 1px solid rgba(255,255,255,0.12);
    border-radius: 20px;
    padding: 2rem;
    display: flex;
    gap: 1.5rem;
    transition: all 0.3s ease;
  }
  .step:hover {
    transform: translateY(-6px);
    border-color: #60a5fa;
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
  }
  .step-number {
    background: #2563eb;
    color: white;
    width: 36px;
    height: 36px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    flex-shrink: 0;
  }
  .step h4 {
    font-size: 1.35rem;
    margin: 0 0 0.8rem;
  }
  .step p {
    color: #cbd5e1;
    line-height: 1.5;
  }

  /* === Ventajas (con hover) === */
  .advantages-box {
    margin-top: 4rem;
    background: rgba(255,255,255,0.08);
    border: 1px solid rgba(255,255,255,0.15);
    border-radius: 24px;
    padding: 3rem;
    transition: all 0.3s ease;
  }
  .advantages-box:hover {
    border-color: #60a5fa;
    transform: translateY(-4px);
  }
  .advantages-box h3 {
    text-align: center;
    margin-bottom: 2rem;
    font-size: 1.6rem;
  }
  .advantages-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 1rem;
  }
  .advantage {
    font-size: 1.1rem;
    padding: 0.8rem 0;
    transition: all 0.2s;
  }
  .advantage:hover {
    color: #93c5fd;
  }

  @media (max-width: 768px) {
    h2 { font-size: 2.4rem; }
    .subtitle { font-size: 1.35rem; }
  }
</style>